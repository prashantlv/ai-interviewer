═══════════════════════════════════════════════════════════════
AI INTERVIEWER BOT AUTO-START IMPLEMENTATION PLAN
═══════════════════════════════════════════════════════════════

📊 DIFFICULTY LEVEL: MEDIUM (30-45 minutes of work)

═══════════════════════════════════════════════════════════════
📋 CHANGES REQUIRED:
═══════════════════════════════════════════════════════════════

1. Modify web_server/routers/dashboard.py (Schedule Interview endpoint)
   Location: create_interview() function
   - After saving interview to DB
   - Spawn bot process using subprocess.Popen()
   - Pass INTERVIEW_ID and WEB_SERVER_URL as env vars
   - Store process PID in a tracking file/dict
   
   Complexity: 🟢 EASY (15 lines of code)
   
   Example code:
   ```python
   import subprocess
   
   # After creating interview in DB
   env = os.environ.copy()
   env['INTERVIEW_ID'] = interview_id
   env['WEB_SERVER_URL'] = 'http://localhost:8009'
   
   process = subprocess.Popen(
       ['python', 'ai-interviewer.py', '--transport', 'daily'],
       cwd='/path/to/server',
       env=env,
       stdout=subprocess.PIPE,
       stderr=subprocess.PIPE
   )
   
   # Store PID
   bot_pids[interview_id] = process.pid
   ```

2. Add process management helper
   Create: web_server/services/bot_manager.py
   
   Functions needed:
   - start_bot(interview_id) -> bool
   - stop_bot(interview_id) -> bool
   - list_active_bots() -> List[Dict]
   - cleanup_finished_bots() -> None
   
   Complexity: 🟡 MEDIUM (50-60 lines of code)
   
   Key features:
   - Track PIDs in memory or Redis
   - Check if process is still running
   - Kill process gracefully (SIGTERM, then SIGKILL)
   - Handle zombie processes

3. Handle conda environment activation
   Problem: Bot needs to run in pipecat-env conda environment
   
   Solutions:
   A. Use conda run:
      ```bash
      conda run -n pipecat-env python ai-interviewer.py --transport daily
      ```
   
   B. Use absolute path to conda python:
      ```python
      python_path = '/home/prashant/miniconda3/envs/pipecat-env/bin/python'
      ```
   
   C. Activate conda in shell script:
      ```bash
      source ~/miniconda3/etc/profile.d/conda.sh
      conda activate pipecat-env
      python ai-interviewer.py --transport daily
      ```
   
   Complexity: 🟡 MEDIUM (tricky part)
   Recommendation: Use absolute path (option B) - simplest and most reliable

4. Add cleanup logic
   - When interview ends (bot sends results), kill bot process
   - Add timeout (e.g., 30 minutes max interview duration)
   - Handle bot crashes gracefully
   - Clean up orphaned processes on web server restart
   
   Complexity: 🟡 MEDIUM (30 lines)
   
   Implementation ideas:
   - Add background task in FastAPI to check bot timeouts
   - Add endpoint: POST /api/bot/cleanup/{interview_id}
   - Call it from bot's on_client_disconnected event

5. Update templates
   Files to modify:
   - web_server/templates/interview_scheduled.html
   - web_server/templates/dashboard.html
   
   Changes:
   - Show "Bot Status: Running/Stopped" 
   - Add "Stop Bot" button if needed
   - Show bot startup logs/errors
   
   Complexity: 🟢 EASY (10 lines)

═══════════════════════════════════════════════════════════════
⚠️ POTENTIAL ISSUES & SOLUTIONS:
═══════════════════════════════════════════════════════════════

1. Process Management
   Issue: Bot runs as separate process, need to track PIDs
   Solution: Use Redis or SQLite to persist PIDs across web server restarts
   Alternative: Accept that restarting web server loses bot tracking

2. Conda Environment
   Issue: subprocess needs to activate conda env
   Solution: Use absolute path to pipecat-env python interpreter
   
   Find it with:
   conda activate pipecat-env
   which python
   # Output: /home/prashant/miniconda3/envs/pipecat-env/bin/python

3. Multiple Interviews
   Issue: Can only run ONE bot at a time (same Daily room)
   Solution: 
   A. Queue interviews - only start bot when previous one ends
   B. Create unique Daily rooms per interview (requires Daily API)
   C. Show error if bot already running
   
   Recommendation: Option C for now (simplest)

4. Error Handling
   Issue: What if bot fails to start?
   Solution:
   - Check if process started successfully (wait 2-3 seconds)
   - Parse bot logs for "Bot ready!" message
   - Show error in dashboard if bot fails
   - Add retry button

5. Resource Management
   Issue: Each bot uses ~500MB RAM, need to ensure cleanup
   Solution:
   - Set max interview duration (30 min)
   - Force kill after timeout
   - On web server startup, kill any orphaned bot processes
   - Monitor system resources

═══════════════════════════════════════════════════════════════
💡 SIMPLER ALTERNATIVES:
═══════════════════════════════════════════════════════════════

OPTION A: Background Service (Recommended for Production)
Description:
- Create a separate bot_scheduler.py service
- Runs continuously in background
- Watches DB for scheduled interviews with status="scheduled"
- Starts bots automatically when interview time arrives
- More robust, easier to manage, decoupled from web server

Implementation:
```python
# bot_scheduler.py
while True:
    interviews = get_scheduled_interviews()
    for interview in interviews:
        if should_start_now(interview):
            start_bot(interview.id)
    time.sleep(10)
```

Pros:
- Cleaner separation of concerns
- Web server doesn't manage bot processes
- Easy to restart either service independently
- Can add scheduling features (start 5 min before)

Cons:
- One more service to run
- More complex deployment

Complexity: 🟡 MEDIUM (40-60 minutes)

---

OPTION B: Manual Start with One-Command (Recommended for Testing)
Description:
- Create a simple CLI helper script
- Recruiter still starts bot manually, but with one command
- No complex process management needed

Implementation:
Create: /home/prashant/Playground/personal/consult/ai-interviewer/start_interview.sh

```bash
#!/bin/bash
# Usage: ./start_interview.sh <interview_id>

INTERVIEW_ID=$1
WEB_SERVER_URL="http://localhost:8009"
SERVER_DIR="/home/prashant/Playground/personal/consult/ai-interviewer/server"
CONDA_ENV="pipecat-env"

if [ -z "$INTERVIEW_ID" ]; then
    echo "Usage: ./start_interview.sh <interview_id>"
    exit 1
fi

echo "🚀 Starting AI Interviewer Bot for: $INTERVIEW_ID"
echo "📍 Room URL: https://hi2inspire.daily.co/hi2inspire"

cd $SERVER_DIR
source ~/miniconda3/etc/profile.d/conda.sh
conda activate $CONDA_ENV

WEB_SERVER_URL=$WEB_SERVER_URL INTERVIEW_ID=$INTERVIEW_ID \
    python ai-interviewer.py --transport daily
```

Usage:
```bash
chmod +x start_interview.sh
./start_interview.sh interview_20251004_153000_abc123
```

Pros:
- Super simple (5 minutes to implement)
- No process management complexity
- Still manual but MUCH easier
- Reliable and predictable

Cons:
- Still requires manual action
- Not fully automated

Complexity: 🟢 EASY (5 minutes)

---

OPTION C: Keep Manual (Current)
Description:
- Most reliable for testing
- You control when bot starts
- No surprises

Command to run:
```bash
cd /home/prashant/Playground/personal/consult/ai-interviewer/server
source ~/miniconda3/etc/profile.d/conda.sh
conda activate pipecat-env
WEB_SERVER_URL=http://localhost:8009 INTERVIEW_ID=<interview_id> \
    python ai-interviewer.py --transport daily
```

Complexity: ✅ DONE (0 minutes)

═══════════════════════════════════════════════════════════════
🎯 RECOMMENDATION:
═══════════════════════════════════════════════════════════════

Development/Testing Phase (NOW):
→ Use OPTION B (One-Command Helper)
  - Quick to implement (5 min)
  - Easy to test
  - Reliable
  - Good for demos

Production Phase (LATER):
→ Implement OPTION A (Background Service)
  - More robust
  - True automation
  - Better for scaling
  - Professional solution

Full Auto-Start (From Dashboard):
→ Implement if needed for specific use case
  - Good for recruiter self-service
  - More complexity
  - Requires careful error handling

═══════════════════════════════════════════════════════════════
📝 IMPLEMENTATION CHECKLIST (When Ready):
═══════════════════════════════════════════════════════════════

Phase 1: One-Command Helper (5 minutes)
□ Create start_interview.sh script
□ Make it executable (chmod +x)
□ Test with a scheduled interview
□ Document usage in README

Phase 2: Full Auto-Start (30-45 minutes)
□ Create web_server/services/bot_manager.py
□ Modify dashboard.py create_interview() to call bot_manager.start_bot()
□ Add PID tracking (in-memory dict or Redis)
□ Test bot startup on interview creation
□ Add error handling and user feedback
□ Add cleanup on timeout
□ Update templates to show bot status

Phase 3: Background Service (40-60 minutes)
□ Create bot_scheduler.py
□ Add DB polling logic
□ Implement bot lifecycle management
□ Add systemd service file for Linux
□ Test service restart scenarios
□ Add monitoring/logging

Phase 4: Polish (20-30 minutes)
□ Add "Stop Bot" button in dashboard
□ Show real-time bot status
□ Add bot logs viewer
□ Implement retry logic
□ Add email notifications on failures
□ Create admin panel for bot management

═══════════════════════════════════════════════════════════════
🔧 TECHNICAL DETAILS:
═══════════════════════════════════════════════════════════════

Finding Conda Python Path:
```bash
conda activate pipecat-env
which python
# Expected: /home/prashant/miniconda3/envs/pipecat-env/bin/python
```

Checking if Bot Process is Running:
```python
import psutil

def is_bot_running(pid):
    try:
        process = psutil.Process(pid)
        return process.is_running() and process.status() != psutil.STATUS_ZOMBIE
    except psutil.NoSuchProcess:
        return False
```

Killing Bot Process:
```python
import signal
import time

def kill_bot(pid):
    try:
        os.kill(pid, signal.SIGTERM)  # Graceful shutdown
        time.sleep(2)
        if is_bot_running(pid):
            os.kill(pid, signal.SIGKILL)  # Force kill
    except ProcessLookupError:
        pass  # Already dead
```

═══════════════════════════════════════════════════════════════
📚 REFERENCES:
═══════════════════════════════════════════════════════════════

Current file locations:
- AI Bot: /home/prashant/Playground/personal/consult/ai-interviewer/server/ai-interviewer.py
- Web Server: /home/prashant/Playground/personal/consult/ai-interviewer/web_server/main.py
- Dashboard Router: /home/prashant/Playground/personal/consult/ai-interviewer/web_server/routers/dashboard.py
- Templates: /home/prashant/Playground/personal/consult/ai-interviewer/web_server/templates/

Environment Variables Required:
- INTERVIEW_ID: Unique identifier for each interview
- WEB_SERVER_URL: http://localhost:8009
- DAILY_API_KEY: (already in .env)
- OPENAI_API_KEY: (already in .env)
- All other keys from server/.env

Daily.co Room:
- Fixed URL: https://hi2inspire.daily.co/hi2inspire
- TODO: Consider unique rooms per interview for parallel interviews

═══════════════════════════════════════════════════════════════
📊 CURRENT STATUS (as of Oct 4, 2025):
═══════════════════════════════════════════════════════════════

✅ WORKING:
- Web server running on port 8009
- Dashboard showing real MongoDB data
- Interview scheduling creates DB records
- /api/bot/interview-result endpoint saves results
- Bot can be started manually and conducts interviews
- Results are properly stored and displayed

❌ NOT WORKING:
- Bot does not auto-start when interview is scheduled
- Recruiter must manually start bot for each interview
- No bot status indicator in dashboard
- No automatic cleanup of finished bots

🎯 NEXT STEP:
Implement OPTION B (One-Command Helper) first for testing,
then OPTION A (Background Service) for production.

═══════════════════════════════════════════════════════════════
END OF DOCUMENT
═══════════════════════════════════════════════════════════════

